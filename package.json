{
  "name": "previa-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:vite": "npm run dev:vite --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:vite": "npm run build:vite --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
